{
  "name": "turbo-gulp.node-lib",
  "version": "1.0.0",
  "description": "Sample node library",
  "private": true,
  "main": "dist/lib/lib/index.js",
  "scripts": {
    "test": "gulp lint && gulp test:clean && gulp test",
    "prepublishOnly": "npm test && gulp lib:dist"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/chai": "^4.1.3",
    "@types/mocha": "^5.2.0",
    "@types/node": "^10.0.3",
    "chai": "^4.1.2",
    "gulp": "^4.0.0",
    "gulp-cli": "^2.0.1"
  },
  "c88": {
    "match": [
      "build/test/lib/*.{js,mjs}",
      "build/test/lib/**/*.{js,mjs}"
    ]
  }
}
